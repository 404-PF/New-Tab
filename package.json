{
  "name": "new-tab",
  "version": "0.4.3",
  "private": true,
  "description": "Chrome/Edge new tab extension",
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "test:coverage": "vitest run --coverage"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^3.0.0",
    "jsdom": "^26.0.0",
    "vitest": "^3.0.0"
  }
}
